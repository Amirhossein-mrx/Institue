export interface StudentShow {
    id: number;
    name: string;
    family: string;
}

export interface StudentInterfaceResult {
    message: string;
}

export interface StudentInterface {
    name: string;
    family: string;
}
